
# 基础命令

```bash
git branch    # 查看当前分支，前面带有‘*’号的就是当前分支

git branch 新的分支名    # 创建新分支
# 注意：该操作只是创建了新分支，并没有切换到新分支

git checkout 分支名     # 切换到某分支
# 注意：可以 checkouot -b 新的分支名 ，实现创建并切换
```

# 分支的合并

```bash
git merge dev01    # 把 dev01 分支合并到 master
```